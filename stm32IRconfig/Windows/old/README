configure and monitor STM32IR

Usage:
stm32IRconfig

program and read the eeprom (wakeups, macros and capabilities)
reset wakeups, macros and alarm
set and get the alarm
reboot
send IR
monitor (read) STM32IR, stop with ^C

