configure and monitor IRMP_STM32

Usage:
stm32IRconfig [/dev/hidraw1]

program and read the eeprom (wakeups, macros and capabilities)
reset wakeups, macros, alarm and eeprom
set and get the alarm
reboot the device
send IR
monitor (read) IRMP_STM32, stop with ^C

put the udev rule from irmplircd into your udev rules directory, then you don't need to give a device
